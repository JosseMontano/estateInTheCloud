
import { ContainerLoader, Loader } from "@/global/styles/modal/perfil";

const Load = () => {
  return (
    <ContainerLoader>
      <Loader></Loader>
    </ContainerLoader>
  );
};

export default Load;
